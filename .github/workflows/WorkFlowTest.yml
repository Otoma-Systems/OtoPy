name: WorkFlow Test

on:
  workflow_dispatch:

jobs:

  deploy:
  
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
        run: |
          git pull
          git describe --tags
